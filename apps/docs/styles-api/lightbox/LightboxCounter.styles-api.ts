import type { LightboxCounterFactory } from "@mantine-bites/lightbox";
import type { StylesApiData } from "../../components/styles-api.types";

export const LightboxCounterStylesApi: StylesApiData<LightboxCounterFactory> = {
	selectors: {
		counter:
			"Element that displays the current slide index and total slide count.",
	},

	vars: {},
};
