import type { LightboxAutoplayButtonFactory } from "@mantine-bites/lightbox";
import type { StylesApiData } from "../../components/styles-api.types";

export const LightboxAutoplayButtonStylesApi: StylesApiData<LightboxAutoplayButtonFactory> =
	{
		selectors: {
			autoplayButton: "Root element that starts and pauses slide autoplay.",
		},

		vars: {},
	};
